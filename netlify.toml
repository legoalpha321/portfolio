# Static site — no build step. Netlify serves the repo root as-is.
[build]
  publish = "."

# Long-cache the static assets, fonts come from the Fontshare CDN.
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"
